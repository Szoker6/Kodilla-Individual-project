'use strict';

/*

(function() {
  
   var Menu = (function() {
     var burger = document.querySelector('.fa');
     
     
     var active = false;
     
     var toggleMenu = function() {
       if (!active) {
         menu.classList.add('asideBar--active');
       
         for (var i = 0, ii = menuItems.length; i < ii; i++) {
           
         }
         
         active = true;
       } else {
         menu.classList.remove('asideBar--active');
      
         for (var i = 0, ii = menuItems.length; i < ii; i++) {
          
         }
         
         active = false;
       }
     };
     
     var bindActions = function() {
       burger.addEventListener('click', toggleMenu, false);
     };
     
     var init = function() {
       bindActions();
     };
     
     return {
       init: init
     };
     
   }());
   
   Menu.init();
   
 }());

 */